{
  "name": "sentinel-server",
  "version": "1.0.0",
  "description": "SENTINEL — ESP32 plain-HTTP + WebSocket push, Node handles all TLS",
  "main": "sentinel-server.js",
  "scripts": {
    "start": "node sentinel-server.js",
    "dev": "node --watch sentinel-server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "undici": "^7.22.0",
    "ws": "^8.16.0",
    "sharp": "^0.33.0"
  },
  "notes": {
    "already_installed": "@supabase/supabase-js, ws, dotenv are already in node_modules",
    "need_npm_install": "express and multer — run: npm install"
  }
}
